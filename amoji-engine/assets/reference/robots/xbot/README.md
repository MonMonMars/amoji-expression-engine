# Mixamo X Bot

`Xbot.glb` from three.js `examples/models/gltf/Xbot.glb` (Mixamo.com character).

Adobe Mixamo animations / characters are royalty-free for commercial use when baked into a product (not redistributed as standalone Mixamo asset packs). See Mixamo FAQ.

Skeleton uses `mixamorig:` bone names — Face Live retarget profile `mixamoColon`.
